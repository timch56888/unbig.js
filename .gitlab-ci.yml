publish:
  image: ${CI_DEPENDENCY_PROXY_GROUP_IMAGE_PREFIX}/node:lts
  stage: deploy
  environment:
    name: $CI_COMMIT_REF_SLUG
  script:
    - npm publish --tag $BRANCH
  only:
    refs:
      - dev
      - alpha
      - beta
      - master
    changes:
      - package.json
